import{_ as t,H as a,f as o,i as s}from"./chunks/framework.D4aZISZY.js";const i="/SeaScan/assets/Metashape_Error.BL95Bh4m.png",n="/SeaScan/assets/ransac_match_325.DYq6Nz-e.jpg",f=JSON.parse('{"title":"","description":"","frontmatter":{},"headers":[],"relativePath":"methodology.md","filePath":"methodology.md"}'),r={name:"methodology.md"};function h(l,e,c,m,d,p){return a(),o("div",null,[...e[0]||(e[0]=[s('<h2 id="introduction" tabindex="-1">Introduction <a class="header-anchor" href="#introduction" aria-label="Permalink to &quot;Introduction&quot;">​</a></h2><p>Mapping software often creates a 3D model of its target before making maps. From this new representation, it can reproject the geometry into new coordinates, such as latitude and longitude.</p><p>To create this 3D model, it estimates where cameras are and what way the cameras are facing (pose estimation). This allows it to fill in tiny details and estimate the 3D structure itself. This process requires the software to find the same objects across multiple images; however, this is not always possible on the ocean. Many parts of the sea surface look exactly like other parts. In these conditions, camera pose estimation often fails. Thus, software like Agisoft Metashape only works for maps in shallow areas.</p><div class="caution custom-block github-alert"><p class="custom-block-title">❌ Metashape Results Over Deep Water</p><p></p><div style="display:grid;place-items:center;"><img src="'+i+'" alt="drawing" width="500"></div> Here is a mesh the ACES team made using an Astro Drone at the Rosenstiel pier. Notice the images in the bottom left corner with no corresponding results. These images were not successfully connected to their surrounding cameras, so the software didn&#39;t know how to align them. </div><p>In this project, we leverage constraints unique to sea-surface mapping to simplify and solve this problem through GPS coordinates and heading information stored in the MAVIC 3E&#39;s metadata. With our method, we can generate maps of much larger and deeper swaths in <strong>tens of minutes</strong>.</p><h2 id="theory" tabindex="-1">Theory <a class="header-anchor" href="#theory" aria-label="Permalink to &quot;Theory&quot;">​</a></h2><p>Structure from motion uses parallax. Imagine you&#39;re in a car; objects closer to the car will appear to move faster than objects farther away. From this, one can ascertain the object&#39;s distance to the car. One can do the same with objects in a photo. Say you have two photos of the same area from a shifted perspective (assume the cameras are pointing the same way, they just moved horizontally), you may be able to shift the images around to align the images. But, due to parallax, this will only align objects at a certain distance from the camera. Normally, this complicates the problem; however, when mapping the sea surface, everything is at the same distance! If we can find a mapping between camera position and image translation, we can stitch images together. This will have the interesting property that it will only focus for this specific distance–objects above or below the water will blur.</p><p>To estimate this transformation, we find targets on the sea surface in a few images and then run an optimization to estimate this transformation.</p><h2 id="methods" tabindex="-1">Methods <a class="header-anchor" href="#methods" aria-label="Permalink to &quot;Methods&quot;">​</a></h2><p>While altitude is often recorded in the metadata of images, it is a complicated measurement that does not necessarily correspond to the distance important here. There could be multiple sources of error, but most importantly, tidal shifts mean that any measurement relative to mean sea level is insufficient. Additionally, the intrinsics of the camera are often not included in the metadata and are important for this function. Moreover, camera intrinsics may change depending on settings, so it may be impractical to measure beforehand.</p><p>Thus, we must estimate this transformation per flight. <strong>This assumes the drone will not fly long enough for a substantial tidal shift.</strong> To do so, on the sea surface, we find shared points across multiple images. We use these targets to find the pixel shifts between images. Then, we can find a linear mapping between GPS shifts and pixel shifts. Conveniently, there are often buoys that fulfill this purpose; however, they can introduce noise if they move significantly.</p><p><img src="'+n+'" alt="RANSAC Example"> We can automatically detect these buoys by their bright color and shape. We can then match buoys by matching the image around them. Large glint areas can greatly decrease the number of buoys found and will limit the matches found. The program requires at least 3 matches to consider pairing good enough for matching. Due to the simple nature of the targets, we create a simple, specialized feature detector and descriptor. Since the matching is simpler than a traditional counterpart like SIFT, we use RANSAC to ensure a consistent image translation. Images that do not have sufficient matches after RANSAC are ignored.</p><p>This could also be done manually. This would require fewer images and only one shared point in each image. Thus, we allow for any target of opportunity to be used.</p><p>After the translations between multiple images are found, the algorithm uses RANSAC to estimate the transformation between GPS coordinates and image translations.</p>',14)])])}const g=t(r,[["render",h]]);export{f as __pageData,g as default};
